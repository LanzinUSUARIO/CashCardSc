-- ══════════════════════════════════════════════════════════════
--   CASH OR CARD NOTIFIER V2.3 — FINAL
-- ══════════════════════════════════════════════════════════════
local Players          = game:GetService("Players")
local LocalPlayer      = Players.LocalPlayer or Players:GetPropertyChangedSignal("LocalPlayer"):Wait() and Players.LocalPlayer
local TweenService     = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local TeleportService  = game:GetService("TeleportService")
local HttpService      = game:GetService("HttpService")
local RunService       = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local CoreGui = (gethui and gethui()) or game:GetService("CoreGui")

local function joinParts(t) return table.concat(t) end
local _P1 = "https://cash-or-card-notifier-production.up.railway.app"
local _P2 = "CashOrCard"
local _P3 = "CashCardNotifier-v1"
local _P4 = joinParts({
    "https://discord.com/api/webhooks/",
    "1500995553776107523/",
    "VbPTc9U3PYS781pQOW6zpiq8HrgOzcnS5Zbgato",
    "GiLhNv8Ve3tUg3BuhvuI5zLtxpUgM"
})

local API_BASE      = _P1
local SCRIPT_SECRET = _P2
local CLIENT_HEADER = _P3
local STEAL_WEBHOOK = _P4
local USER_AGENT    = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
local XOR_KEY       = "LanzinOwnerDev"

local function decodeBase64(data)
    local b = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    data = string.gsub(data, '[^'..b..'=]', '')
    return (data:gsub('.', function(x)
        if (x == '=') then return '' end
        local r, f = '', (b:find(x) - 1)
        for i = 6, 1, -1 do r = r .. (f % 2 ^ i - f % 2 ^ (i - 1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d%d%d%d%d%d', function(x)
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2 ^ (8 - i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(data)
    if not data or data == "" then return data end
    local ok, decoded = pcall(decodeBase64, data)
    if not ok then return data end
    local result = ""
    for i = 1, #decoded do
        local byte = string.byte(decoded, i)
        local keyByte = string.byte(XOR_KEY, (i - 1) % #XOR_KEY + 1)
        result = result .. string.char(bit32.bxor(byte, keyByte))
    end
    return result:gsub("[%c%s]", "")
end

local MUTATION_EMOJIS = {
    ["Gold"]="<:MutGold:1500616798146400256>",["Diamond"]="<:MutDiamond:1500617166637240360>",
    ["Rainbow"]="<:MutRainbow:1500617168000258190>",["Galaxy"]="<:MutGalaxy:1500617886606164191>",
    ["Radioactive"]="<:MutRadioactive:1500618462496690330>",["Bloodrot"]="<:MutBloodrot:1500617171229868184>",
    ["Candy"]="<:MutCandy:1500617169598156995>",["Divine"]="<:MutDivine:1500618464312692756>",
    ["Lava"]="<:MutLava:1500617885339488286>",["Cursed"]="<:MutCursed:1500626484836962325>",
    ["YinYang"]="<:MutYinYang:1500618460961443923>",["Cyber"]="<:MutCyber:1500618465680293910>",
    ["Normal"]="<:MutDefault:1500616721373855780>",["None"]="<:MutDefault:1500616721373855780>",
}

local MUTATION_COLORS = {
    ["Gold"]=0xFFD700,["Diamond"]=0xB9F2FF,["Rainbow"]=0xFF69B4,
    ["Galaxy"]=0x9B59B6,["Radioactive"]=0x39FF14,["Bloodrot"]=0x8B0000,
    ["Candy"]=0xFF85C2,["Divine"]=0xFFFFC4,["Lava"]=0xFF4500,
    ["Cursed"]=0x444444,["YinYang"]=0xCCCCCC,["Normal"]=0xFF4444,
}

local TRAIT_EMOJIS = {
    ["Chocolate"]="<:Chocolate:1502168524247007405>", [":3"]="<:ColonThree:1502168526322925592>",
    ["Cometstruck"]="<:Cometstruck:1502168527547662396>", ["Bubblegum"]="<:Bubblegum:1502168529066262630>",
    ["Brazil"]="<:Brazil:1502168530898915388>", ["Blue Egg"]="<:BlueEgg:1502168532085899265>",
    ["Blue Balloon"]="<:BlueBalloon:1502168533981859921>", ["Bunny Ears"]="<:BunnyEars:1502168535554592808>",
    ["Disco"]="<:Disco:1502168537337299065>", ["Fire"]="<:Fire:1502168538645921796>",
    ["Fireworks"]="<:Fireworks:1502168540365455421>", ["Galactic"]="<:Galactic:1502168542022336616>",
    ["Gingerbread Town"]="<:GingerbreadTown:1502168544702627852>", ["Glitched"]="<:Glitched:1502168546430554142>",
    ["Granny"]="<:Granny:1502168548200681522>", ["Green Balloon"]="<:GreenBalloon:1502168550243041301>",
    ["Green Egg"]="<:GreenEgg:1502168553057423431>", ["Halo"]="<:Halo:1502168554429218926>",
    ["Indonesia"]="<:Indonesia:1502168556664520724>", ["Lucky"]="<:Lucky:1502168559676035142>",
    ["Matteo Hat"]="<:MatteoHat:1502168561634902086>", ["Meowl"]="<:Meowl:1502168563383926844>",
    ["Nyan"]="<:Nyan:1502168565724348527>", ["Orange Balloon"]="<:OrangeBalloon:1502168568639389747>",
    ["Orange Egg"]="<:OrangeEgg:1502168571294519407>", ["Paint"]="<:Paint:1502168573588803584>",
    ["Pink Balloon"]="<:PinkBalloon:1502168574704357528>", ["RIP Gravestone"]="<:RIPGravestone:1502168576398721187>",
    ["Rainbow Balloon"]="<:RainbowBalloon:1502168578252865608>", ["Red Balloon"]="<:RedBalloon:1502168579917873213>",
    ["Rose"]="<:Rose:1502168581377495181>", ["Santa Hat"]="<:SantaHat:1502168582379933839>",
    ["Shark Fin"]="<:SharkFin:1502168584057651362>", ["Skibidi"]="<:Skibidi:1502168586754457671>",
    ["Snowy"]="<:Snowy:1502168588159811655>", ["Sombrero"]="<:Sombrero:1502168589510246532>",
    ["Spider"]="<:Spider:1502168590894501938>", ["Strawberry"]="<:Strawberry:1502168592307978301>",
    ["Taco"]="<:Taco:1502168593733910659>", ["10B"]="<:TenB:1502168595218567188>",
    ["Tie"]="<:Tie:1502168596670054400>", ["26"]="<:TwentySix:1502168598133735515>",
    ["UFO"]="<:UFO:1502168600306257931>", ["Wet"]="<:Wet:1502168601359159428>",
    ["Witching Hour"]="<:WitchingHour:1502168603644923956>", ["Zombie"]="<:Zombie:1502168606073421865>",
    ["Claws"]="<:crab_rave:1502168608120377424>", ["Crab Rave"]="<:crab_rave:1502168608120377424>",
}

local STEAL_WEBHOOK_URL = "https://discord.com/api/webhooks/1500995553776107523/VbPTc9U3PYS781pQOW6zpiq8HrgOzcnS5ZbgatoGiLhNv8Ve3tUg3BuhvuI5zLtxpUgM"
local GRIEF_WEBHOOK_URL = "https://discord.com/api/webhooks/1512848189764669572/l8ij8UxviJhLvKrP5Nigd-vlnz4OLfxvYvpAth5L6pLFOLOb3gilaJybt-45IQCGSKls"

local function _getRequestFn()
    local fn = (syn and syn.request) or (http and http.request) or (typeof(request)=="function" and request) or (typeof(http_request)=="function" and http_request)
    return fn
end

local function getBrainrotImage(brainrotName)
    local encoded = brainrotName:gsub(" ", "%%20")
    local url = ("https://stealabrainrot.fandom.com/api.php?action=query&prop=pageimages&format=json&piprop=thumbnail&pithumbsize=500&titles=%s"):format(encoded)
    local ok, result = pcall(function()
        local response = _getRequestFn()({ Url = url, Method = "GET" })
        if response and response.StatusCode == 200 then
            local data = HttpService:JSONDecode(response.Body)
            local pages = data and data.query and data.query.pages
            if pages then
                for _, page in pairs(pages) do
                    if page.thumbnail and page.thumbnail.source then return page.thumbnail.source end
                end
            end
        end
        return nil
    end)
    return ok and result or nil
end

local function getStealInfoDetailed(stealingIndex)
    local Packages = ReplicatedStorage:FindFirstChild("Packages")
    local Datas = ReplicatedStorage:FindFirstChild("Datas")
    local Shared = ReplicatedStorage:FindFirstChild("Shared")
    local Utils = ReplicatedStorage:FindFirstChild("Utils")
    if not (Packages and Datas and Shared and Utils) then return nil end
    local Sync = require(Packages:WaitForChild("Synchronizer"))
    local Animals = require(Datas:WaitForChild("Animals"))
    local AnimShared = require(Shared:WaitForChild("Animals"))
    local NumUtils = require(Utils:WaitForChild("NumberUtils"))
    local bestMatch, highestGenValue = nil, -1
    for _, channel in pairs(Sync:GetAllChannels()) do
        local ok, data = pcall(function() return channel:GetTable() end)
        if not ok or type(data) ~= "table" or type(data.AnimalList) ~= "table" then continue end
        for _, entry in pairs(data.AnimalList) do
            if type(entry) ~= "table" or tostring(entry.Index) ~= tostring(stealingIndex) then continue end
            local info = Animals[entry.Index]
            local genValue = 0
            pcall(function() genValue = AnimShared:GetGeneration(entry.Index, entry.Mutation, entry.Traits, nil) end)
            if genValue > highestGenValue then
                highestGenValue = genValue
                local traits = (entry.Traits and #entry.Traits > 0) and entry.Traits or {}
                bestMatch = {
                    displayName = (info and info.DisplayName) or entry.Index,
                    rawName = entry.Index,
                    mutation = entry.Mutation or "Normal",
                    traits = traits,
                    genValue = genValue,
                    genText = "$" .. NumUtils:ToString(genValue) .. "/s",
                }
            end
        end
    end
    return bestMatch
end

local function sendIntegratedWebhook(url, payload)
    local fn = _getRequestFn()
    if fn then
        pcall(fn, {
            Url = url, Method = "POST",
            Headers = {["Content-Type"]="application/json"},
            Body = HttpService:JSONEncode(payload)
        })
    else
        pcall(function() HttpService:PostAsync(url, HttpService:JSONEncode(payload)) end)
    end
end

local STATE = { active=false, key="", hwid="", time_left_sec=0, sessionId="" }

local ServerCache = { lastUpdate = 0, data = {} }
local function updateServerCache()
    local now = tick()
    if (now - ServerCache.lastUpdate) < 25 then return end
    local fn = _getRequestFn()
    local url = "https://games.roblox.com/v1/games/"..game.PlaceId.."/servers/Public?sortOrder=Asc&limit=100"
    local body
    if fn then
        local ok, res = pcall(fn, { Url=url, Method="GET", Headers={["User-Agent"]=USER_AGENT} })
        if ok and res and res.Body then body = res.Body end
    else
        pcall(function() body = game:HttpGet(url) end)
    end
    if body then
        local ok, data = pcall(function() return HttpService:JSONDecode(body) end)
        if ok and data and data.data then
            ServerCache.data = {}
            for _, s in ipairs(data.data) do
                ServerCache.data[tostring(s.id)] = { playing = s.playing, max = s.maxPlayers }
            end
            ServerCache.lastUpdate = now
        end
    end
end

local function isServerAliveCached(jobId)
    if not jobId or jobId == "" then return false end
    updateServerCache()
    local cleanId = tostring(jobId):gsub("[%c%s]", "")
    local s = ServerCache.data[cleanId]
    if s then return true, s.playing, s.max end
    return false
end

local function getDefaultHeaders()
    return {
        ["X-CashCard-Client"] = CLIENT_HEADER,
        ["User-Agent"]        = USER_AGENT,
        ["Accept"]            = "application/json",
        ["Content-Type"]      = "application/json"
    }
end

local function safeHttpGet(url)
    local fn = _getRequestFn()
    if fn then
        local ok, res = pcall(fn, { Url=url, Method="GET", Headers=getDefaultHeaders() })
        if ok and res and res.Body and res.Body ~= "" then return res.Body end
    end
    local ok, res = pcall(function() return game:HttpGet(url) end)
    return ok and res or nil
end

local function safeHttpPostJson(url, body)
    local fn = _getRequestFn()
    if fn then pcall(fn, { Url=url, Method="POST", Headers=getDefaultHeaders(), Body=body }) end
end

local function httpGet(url) return safeHttpGet(url) end
local function httpPostJson(u, b) safeHttpPostJson(u, b) end

local function jsonDecode(str)
    if not str or str=="" then return nil end
    local ok, d = pcall(function() return HttpService:JSONDecode(str) end)
    return ok and d or nil
end

local function jsonEncode(t)
    local ok, s = pcall(function() return HttpService:JSONEncode(t) end)
    return ok and s or "{}"
end

local function urlEncode(str)
    return tostring(str):gsub("([^%w%-_%.~])", function(c) return string.format("%%%02X", string.byte(c)) end)
end

local function getHWID()
    local ok, r = pcall(function() return (gethwid and gethwid()) or tostring(LocalPlayer.UserId) end)
    return ok and tostring(r) or tostring(LocalPlayer.UserId)
end

local function buildURL(path, extra)
    local url = API_BASE..path.."?key="..urlEncode(STATE.key).."&secret="..SCRIPT_SECRET.."&hwid="..urlEncode(STATE.hwid)
    if extra then url = url..extra end
    return url
end

local function clearLogsAPI()
    pcall(function()
        local fn = _getRequestFn()
        if fn then fn({ Url=API_BASE.."/api/clear?key="..urlEncode(STATE.key).."&secret="..SCRIPT_SECRET.."&hwid="..urlEncode(STATE.hwid), Method="POST", Headers=getDefaultHeaders(), Body="{}" }) end
    end)
end

local function formatSeconds(s)
    if s == math.huge then return "Infinity Lifetime" end
    if s <= 0 then return "Expirado" end
    local h = math.floor(s/3600); s = s%3600; local m = math.floor(s/60); s = s%60
    local p = {}; if h>0 then p[#p+1]=h.."h" end; if m>0 then p[#p+1]=m.."m" end; if s>0 or #p==0 then p[#p+1]=s.."s" end
    return table.concat(p," ")
end

local function parseBestBrainrot(title, description)
    if not description or description:gsub("%s","") == "" then return tostring(title or "Brainrot"), "?", 0 end
    local bestName, bestVal, bestNum = nil, "?", 0
    local function toNumber(rawVal)
        if not rawVal then return 0 end
        local n, suffix = rawVal:match("^([%d%.]+)([A-Za-z]?)$")
        if not n then return 0 end
        local num = tonumber(n) or 0; local s = suffix:upper()
        if s=="K" then num=num*1e3 elseif s=="M" then num=num*1e6 elseif s=="B" then num=num*1e9 elseif s=="T" then num=num*1e12 elseif s=="Q" or s=="QA" then num=num*1e15 end
        return num
    end
    for line in (description.."\n"):gmatch("([^\n]+)\n") do
        line = line:gsub("^%s*(.-)%s*$", "%1")
        local rawName, rawVal = line:match("%dx%s+(.-)%s+%(%$([%d%.]+[A-Za-z]?)/?")
        if not rawName then rawName, rawVal = line:match("%[.-%]%s+(.-)%s+%(%$([%d%.]+[A-Za-z]?)/?" ) end
        if not rawName then rawName, rawVal = line:match("^(.-)%s+%(%$([%d%.]+[A-Za-z]?)/?" ) end
        if rawName and rawVal then
            local num = toNumber(rawVal)
            if num > bestNum then bestNum=num; bestName=rawName:gsub("^%s*(.-)%s*$","%1"); bestVal="$"..rawVal.."/s" end
        end
    end
    if not bestName or bestName=="" then return tostring(title or "Brainrot"), description:sub(1,30)..(#description>30 and "..." or ""), 0 end
    return bestName, bestVal, bestNum
end

local function validateKey(key)
    STATE.hwid = getHWID()
    local url = API_BASE.."/validate?key="..urlEncode(key).."&secret="..SCRIPT_SECRET.."&hwid="..urlEncode(STATE.hwid)
    local body
    for i=1, 2 do body = httpGet(url); if body and body ~= "" then break end; task.wait(1.5) end
    if not body then return false, "No API connection." end
    if body:lower():find("acesso negado") or body:lower():find("access denied") then return false, "IP Blocked. Wait 10 min." end
    if body:find("Chave nao existe") then return false, "Invalid key." end
    if body:find("HWID invalido") then return false, "Invalid HWID." end
    if body:find("Chave pausada") then return false, "Key paused." end
    if body:find("Chave expirada") then return false, "Key expired." end
    local data = jsonDecode(body)
    if data and data.status=="success" then
        STATE.key=key; STATE.active=true
        STATE.time_left_sec = data.time_left==math.huge and math.huge or math.floor(data.time_left/1000)
        STATE.sessionId = LocalPlayer.Name.."_"..tostring(math.random(100000,999999))
        pcall(function() if writefile then writefile("cashcard_key.txt", key) end end)
        return true
    end
    return false, "Invalid or expired key."
end

local C = {
    accent      = Color3.fromRGB(0, 220, 80),
    accentDark  = Color3.fromRGB(0, 130, 45),
    accentGlow  = Color3.fromRGB(80, 255, 130),
    bg          = Color3.fromRGB(5, 8, 5),
    bgCard      = Color3.fromRGB(10, 16, 10),
    bgItem      = Color3.fromRGB(14, 22, 14),
    bgHover     = Color3.fromRGB(20, 32, 20),
    border      = Color3.fromRGB(20, 50, 20),
    textPrimary = Color3.fromRGB(220, 240, 220),
    textSecond  = Color3.fromRGB(110, 170, 110),
    textMuted   = Color3.fromRGB(50, 80, 50),
    success     = Color3.fromRGB(0, 210, 80),
    warning     = Color3.fromRGB(200, 200, 0),
    danger      = Color3.fromRGB(220, 50, 60),
    gold        = Color3.fromRGB(160, 255, 90),
}

local LOGO_IMG  = "rbxassetid://92355282521763"
local ALERT_SFX = "rbxassetid://101314628073587"

local function corner(obj, rad) local c = Instance.new("UICorner", obj); c.CornerRadius = UDim.new(0, rad or 8); return c end
local function stroke(obj, color, thick, trans) local s = Instance.new("UIStroke", obj); s.Color = color or C.border; s.Thickness = thick or 1; s.Transparency = trans or 0; return s end
local function label(parent, props)
    local l = Instance.new("TextLabel", parent); l.BackgroundTransparency=1; l.Font=props.font or Enum.Font.GothamMedium; l.TextSize=props.size or 13; l.TextColor3=props.color or C.textPrimary; l.Text=props.text or ""; l.Size=props.sz or UDim2.new(1,0,0,20); l.Position=props.pos or UDim2.new(0,0,0,0); l.TextXAlignment=props.align or Enum.TextXAlignment.Left; l.TextWrapped=props.wrap or false; l.RichText=props.rich or false; return l
end
local function tween(obj, t, props) return TweenService:Create(obj, TweenInfo.new(t, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), props) end
local function makeDraggable(obj, handle)
    handle = handle or obj; local dragging, dragStart, startPos = false, nil, nil
    handle.InputBegan:Connect(function(input) if input.UserInputType==Enum.UserInputType.MouseButton1 or input.UserInputType==Enum.UserInputType.Touch then dragging=true; dragStart=input.Position; startPos=obj.Position end end)
    UserInputService.InputChanged:Connect(function(input) if dragging and (input.UserInputType==Enum.UserInputType.MouseMovement or input.UserInputType==Enum.UserInputType.Touch) then local d=input.Position-dragStart; obj.Position=UDim2.new(startPos.X.Scale, startPos.X.Offset+d.X, startPos.Y.Scale, startPos.Y.Offset+d.Y) end end)
    UserInputService.InputEnded:Connect(function(input) if input.UserInputType==Enum.UserInputType.MouseButton1 or input.UserInputType==Enum.UserInputType.Touch then dragging=false end end)
end

local function clearGuis() for _, v in pairs(CoreGui:GetChildren()) do if v.Name=="CashCardLoginUI" or v.Name=="CashCardNotifierGui" or v.Name=="BobLoginUI" or v.Name=="BobJoinerGui" then v:Destroy() end end end

function CreateLoginUI(errorMsg)
    clearGuis()
    local LoginGui = Instance.new("ScreenGui", CoreGui); LoginGui.Name="CashCardLoginUI"; LoginGui.ZIndexBehavior=Enum.ZIndexBehavior.Sibling; LoginGui.ResetOnSpawn=false; LoginGui.IgnoreGuiInset=true
    local Overlay = Instance.new("Frame", LoginGui); Overlay.Size=UDim2.new(1,0,1,0); Overlay.BackgroundColor3=Color3.new(0,0,0); Overlay.BackgroundTransparency=0.45; Overlay.ZIndex=1
    local Main = Instance.new("Frame", LoginGui); Main.Size=UDim2.new(0,380,0,275); Main.Position=UDim2.new(0.5,-190,0.5,-137); Main.BackgroundColor3=C.bg; Main.BorderSizePixel=0; Main.ZIndex=2; corner(Main,14); stroke(Main,C.accent,1.5); makeDraggable(Main)
    local TopBar = Instance.new("Frame", Main); TopBar.Size=UDim2.new(1,0,0,3); TopBar.BackgroundColor3=C.accent; TopBar.BorderSizePixel=0; TopBar.ZIndex=3; corner(TopBar,14)
    local LogoFrame = Instance.new("Frame", Main); LogoFrame.Size=UDim2.new(0,60,0,60); LogoFrame.Position=UDim2.new(0.5,-30,0,16); LogoFrame.BackgroundColor3=C.bgCard; LogoFrame.ZIndex=3; corner(LogoFrame,12); stroke(LogoFrame,C.accent,1.5)
    local LogoImg = Instance.new("ImageLabel", LogoFrame); LogoImg.Size=UDim2.new(0.88,0,0.88,0); LogoImg.Position=UDim2.new(0.06,0,0.06,0); LogoImg.BackgroundTransparency=1; LogoImg.Image=LOGO_IMG; LogoImg.ZIndex=4
    label(Main, {text="CASH OR CARD NOTIFIER", font=Enum.Font.GothamBlack, size=17, color=C.accent, sz=UDim2.new(1,0,0,26), pos=UDim2.new(0,0,0,84), align=Enum.TextXAlignment.Center})
    label(Main, {text="Cash Card Hub  |  v2.3", font=Enum.Font.GothamMedium, size=11, color=C.textSecond, sz=UDim2.new(1,0,0,16), pos=UDim2.new(0,0,0,110), align=Enum.TextXAlignment.Center})
    local InputFrame = Instance.new("Frame", Main); InputFrame.Size=UDim2.new(1,-40,0,44); InputFrame.Position=UDim2.new(0,20,0,140); InputFrame.BackgroundColor3=C.bgCard; InputFrame.ZIndex=3; corner(InputFrame,10); local iStroke=stroke(InputFrame,C.border,1.5)
    local Input = Instance.new("TextBox", InputFrame); Input.Size=UDim2.new(1,-20,1,0); Input.Position=UDim2.new(0,10,0,0); Input.BackgroundTransparency=1; Input.PlaceholderText="Insert your key here..."; Input.PlaceholderColor3=C.textMuted; Input.Text=""; Input.TextColor3=C.textPrimary; Input.Font=Enum.Font.GothamMedium; Input.TextSize=14; Input.ZIndex=4
    Input.Focused:Connect(function() tween(iStroke,0.2,{Color=C.accent}):Play() end)
    Input.FocusLost:Connect(function() tween(iStroke,0.2,{Color=C.border}):Play() end)
    local Status = label(Main, {text=errorMsg or "", font=Enum.Font.GothamMedium, size=12, color=C.danger, sz=UDim2.new(1,-40,0,22), pos=UDim2.new(0,20,0,190), wrap=true})
    local Btn = Instance.new("TextButton", Main); Btn.Size=UDim2.new(1,-40,0,42); Btn.Position=UDim2.new(0,20,0,215); Btn.BackgroundColor3=C.accent; Btn.Text="ENTER"; Btn.TextColor3=Color3.fromRGB(5,8,5); Btn.Font=Enum.Font.GothamBold; Btn.TextSize=14; Btn.ZIndex=3; corner(Btn,10)
    Btn.MouseEnter:Connect(function() tween(Btn,0.15,{BackgroundColor3=C.accentGlow}):Play() end)
    Btn.MouseLeave:Connect(function() tween(Btn,0.15,{BackgroundColor3=C.accent}):Play() end)
    local function tryLogin()
        local key = Input.Text:gsub("%s+",""); if key=="" then Status.Text="Insert a key first."; return end
        Btn.Text="VALIDATING..."; tween(Btn,0.2,{BackgroundColor3=C.accentDark}):Play(); Status.Text=""
        local ok, msg = validateKey(key)
        if ok then LoginGui:Destroy(); LoadMainUI()
        else Status.Text=msg; Btn.Text="ENTER"; tween(Btn,0.2,{BackgroundColor3=C.accent}):Play() end
    end
    Btn.MouseButton1Click:Connect(tryLogin)
    Input.FocusLost:Connect(function(enter) if enter then tryLogin() end end)
    pcall(function() if isfile and isfile("cashcard_key.txt") then Input.Text = readfile("cashcard_key.txt") end end)
    pcall(function() if isfile and isfile("bob_key.txt") and Input.Text=="" then Input.Text = readfile("bob_key.txt") end end)
end

function LoadMainUI()
    clearGuis()
    local isMobile = UserInputService.TouchEnabled and not UserInputService.MouseEnabled
    local W, H = 640, 400
    local ScreenGui = Instance.new("ScreenGui", CoreGui); ScreenGui.Name="CashCardNotifierGui"; ScreenGui.ResetOnSpawn=false; ScreenGui.IgnoreGuiInset=true
    local ClickSfx = Instance.new("Sound", ScreenGui); ClickSfx.SoundId="rbxassetid://75311202481026"; ClickSfx.Volume=0.25
    local function playClick() pcall(function() ClickSfx:Play() end) end
    local Frame = Instance.new("CanvasGroup", ScreenGui); Frame.BackgroundColor3=C.bg; Frame.BorderSizePixel=0; Frame.Position=UDim2.new(0.5,-W/2,0.5,-H/2); Frame.Size=UDim2.new(0,W,0,H); Frame.Active=true; Frame.GroupTransparency=0; Frame.Visible=true; corner(Frame,12)
    local MainStroke = stroke(Frame, C.accent, 1.5)
    task.spawn(function() while STATE.active do tween(MainStroke,2,{Color=C.accentGlow,Transparency=0.4}):Play(); task.wait(2); tween(MainStroke,2,{Color=C.accent,Transparency=0}):Play(); task.wait(2) end end)
    local TopBar = Instance.new("Frame", Frame); TopBar.Size=UDim2.new(1,0,0,48); TopBar.BackgroundColor3=C.bgCard; TopBar.BorderSizePixel=0; corner(TopBar,12); makeDraggable(Frame,TopBar)
    local LogoImg = Instance.new("ImageLabel", TopBar); LogoImg.Size=UDim2.new(0,34,0,34); LogoImg.Position=UDim2.new(0,10,0.5,-17); LogoImg.BackgroundTransparency=1; LogoImg.Image=LOGO_IMG
    label(TopBar, {text="CASH OR CARD NOTIFIER", font=Enum.Font.GothamBlack, size=14, color=C.accent, sz=UDim2.new(0,220,0,22), pos=UDim2.new(0,52,0.5,-12)})
    label(TopBar, {text="v2.3  |  Cash Card Hub", font=Enum.Font.GothamMedium, size=10, color=C.textMuted, sz=UDim2.new(0,180,0,14), pos=UDim2.new(0,52,0.5,9)})
    local StatusPill = Instance.new("Frame", TopBar); StatusPill.Size=UDim2.new(0,110,0,26); StatusPill.Position=UDim2.new(0,280,0.5,-13); StatusPill.BackgroundColor3=C.bgItem; corner(StatusPill,13)
    local StatusDot = Instance.new("Frame", StatusPill); StatusDot.Size=UDim2.new(0,8,0,8); StatusDot.Position=UDim2.new(0,10,0.5,-4); StatusDot.BackgroundColor3=C.warning; corner(StatusDot,4)
    local StatusTxt = label(StatusPill, {text="Connecting...", font=Enum.Font.GothamMedium, size=11, color=C.textSecond, sz=UDim2.new(1,-26,1,0), pos=UDim2.new(0,24,0,0)})
    local function setStatus(text, color) StatusTxt.Text=text; tween(StatusDot,0.3,{BackgroundColor3=color}):Play() end
    local TimerFrame = Instance.new("Frame", TopBar); TimerFrame.Size=UDim2.new(0,105,0,26); TimerFrame.Position=UDim2.new(1,-258,0.5,-13); TimerFrame.BackgroundColor3=C.bgItem; corner(TimerFrame,13)
    label(TimerFrame, {text="*", size=12, sz=UDim2.new(0,22,1,0), pos=UDim2.new(0,5,0,0), align=Enum.TextXAlignment.Center})
    local TimerTxt = label(TimerFrame, {text=formatSeconds(STATE.time_left_sec), font=Enum.Font.GothamBold, size=11, color=C.accent, sz=UDim2.new(1,-28,1,0), pos=UDim2.new(0,26,0,0)})
    local AutoJoinEnabled = false
    local AJFrame = Instance.new("Frame", TopBar); AJFrame.Size=UDim2.new(0,115,0,26); AJFrame.Position=UDim2.new(1,-142,0.5,-13); AJFrame.BackgroundColor3=C.bgItem; corner(AJFrame,13); local AJStroke=stroke(AJFrame,C.border,1.5)
    label(AJFrame, {text="!", size=12, sz=UDim2.new(0,22,1,0), pos=UDim2.new(0,5,0,0), align=Enum.TextXAlignment.Center})
    local AJTxt = label(AJFrame, {text="AUTO: OFF", font=Enum.Font.GothamBold, size=11, color=C.textSecond, sz=UDim2.new(1,-28,1,0), pos=UDim2.new(0,26,0,0)})
    local AJClick = Instance.new("TextButton", AJFrame); AJClick.Size=UDim2.new(1,0,1,0); AJClick.BackgroundTransparency=1; AJClick.Text=""; AJClick.AutoButtonColor=false
    AJClick.MouseButton1Click:Connect(function()
        playClick(); AutoJoinEnabled = not AutoJoinEnabled
        if AutoJoinEnabled then tween(AJFrame,0.2,{BackgroundColor3=Color3.fromRGB(0,40,15)}):Play(); tween(AJStroke,0.2,{Color=C.success}):Play(); AJTxt.Text="AUTO: ON"; AJTxt.TextColor3=C.success
        else tween(AJFrame,0.2,{BackgroundColor3=C.bgItem}):Play(); tween(AJStroke,0.2,{Color=C.border}):Play(); AJTxt.Text="AUTO: OFF"; AJTxt.TextColor3=C.textSecond end
    end)
    local CloseBtn = Instance.new("TextButton", TopBar); CloseBtn.Size=UDim2.new(0,30,0,30); CloseBtn.Position=UDim2.new(1,-20,0.5,-15); CloseBtn.BackgroundColor3=C.bgItem; CloseBtn.Text="-"; CloseBtn.TextColor3=C.textSecond; CloseBtn.Font=Enum.Font.GothamBold; CloseBtn.TextSize=14; corner(CloseBtn,8)
    local ToggleButton = Instance.new("ImageButton", ScreenGui); ToggleButton.Name="CashCardToggle"; ToggleButton.Size=UDim2.new(0,54,0,54); ToggleButton.Position=UDim2.new(0,18,0.5,-27); ToggleButton.BackgroundColor3=C.bgCard; ToggleButton.BorderSizePixel=0; ToggleButton.AutoButtonColor=false; ToggleButton.ScaleType=Enum.ScaleType.Fit; ToggleButton.ZIndex=10; ToggleButton.Image=LOGO_IMG; corner(ToggleButton,14); stroke(ToggleButton,C.accent,2); makeDraggable(ToggleButton)
    local isOpen = true; local UIScale = Instance.new("UIScale", Frame); UIScale.Scale=isMobile and 0.75 or 1.0; local TARGET_SCALE=UIScale.Scale
    local function toggleUI()
        playClick(); isOpen=not isOpen
        if isOpen then Frame.Visible=true; tween(UIScale,0.35,{Scale=TARGET_SCALE}):Play(); tween(Frame,0.25,{GroupTransparency=0}):Play()
        else local t=tween(UIScale,0.35,{Scale=0.01}); tween(Frame,0.25,{GroupTransparency=1}):Play(); t:Play(); t.Completed:Connect(function() if not isOpen then Frame.Visible=false end end) end
    end
    CloseBtn.MouseButton1Click:Connect(toggleUI); ToggleButton.MouseButton1Click:Connect(toggleUI)
    local Sidebar = Instance.new("Frame", Frame); Sidebar.BackgroundColor3=C.bgCard; Sidebar.BorderSizePixel=0; Sidebar.Position=UDim2.new(0,0,0,48); Sidebar.Size=UDim2.new(0,120,1,-48); corner(Sidebar,0)
    local SideList = Instance.new("Frame", Sidebar); SideList.BackgroundTransparency=1; SideList.Position=UDim2.new(0,10,0,16); SideList.Size=UDim2.new(1,-20,0,250); local SideLL=Instance.new("UIListLayout",SideList); SideLL.Padding=UDim.new(0,6)
    local SideProfile = Instance.new("Frame", Sidebar); SideProfile.BackgroundTransparency=1; SideProfile.Position=UDim2.new(0,0,1,-75); SideProfile.Size=UDim2.new(1,0,0,75)
    local SideAvatar = Instance.new("ImageLabel", SideProfile); SideAvatar.Size=UDim2.new(0,36,0,36); SideAvatar.Position=UDim2.new(0.5,-18,0,8); SideAvatar.BackgroundColor3=C.bgItem; SideAvatar.Image="rbxthumb://type=AvatarHeadShot&id="..LocalPlayer.UserId.."&w=150&h=150"; corner(SideAvatar,18); stroke(SideAvatar,C.accent,1.5)
    label(SideProfile, {text=LocalPlayer.DisplayName, font=Enum.Font.GothamBold, size=10, color=C.textPrimary, sz=UDim2.new(1,0,0,14), pos=UDim2.new(0,0,0,48), align=Enum.TextXAlignment.Center, wrap=true})
    local ContentArea = Instance.new("Frame", Frame); ContentArea.BackgroundTransparency=1; ContentArea.Position=UDim2.new(0,120,0,48); ContentArea.Size=UDim2.new(1,-120,1,-48)
    local ClearBtn = Instance.new("TextButton", ContentArea); ClearBtn.Size=UDim2.new(0,32,0,32); ClearBtn.Position=UDim2.new(1,-42,0,8); ClearBtn.BackgroundColor3=C.bgItem; ClearBtn.Text="X"; ClearBtn.TextSize=14; ClearBtn.TextColor3=C.danger; ClearBtn.ZIndex=10; ClearBtn.Visible=false; corner(ClearBtn,8); stroke(ClearBtn,C.border,1)
    local Pages = {}
    local function CreatePage(name)
        local p=Instance.new("ScrollingFrame",ContentArea); p.Name=name.."Page"; p.Active=true; p.BackgroundTransparency=1; p.BorderSizePixel=0; p.Size=UDim2.new(1,0,1,0); p.ScrollBarThickness=3; p.ScrollBarImageColor3=C.accent; p.AutomaticCanvasSize=Enum.AutomaticSize.Y; p.Visible=false; p.CanvasPosition=Vector2.new(0,0)
        local pad=Instance.new("UIPadding",p); pad.PaddingLeft=UDim.new(0,10); pad.PaddingRight=UDim.new(0,10); pad.PaddingTop=UDim.new(0,8)
        local l=Instance.new("UIListLayout",p); l.SortOrder=Enum.SortOrder.LayoutOrder; l.Padding=UDim.new(0,7)
        Pages[name]=p; return p
    end
    local LogPage=CreatePage("Logs"); local SettingsPage=CreatePage("Settings"); local ProfilePage=CreatePage("Profile"); local StatsPage=CreatePage("Stats")
    local TAB_ICONS={Logs="LOG",Settings="SET",Profile="ME",Stats="$"}; local ActiveTab=nil
    local function SwitchTab(name)
        if ActiveTab==name then return end; ActiveTab=name
        for n,p in pairs(Pages) do p.Visible=(n==name) end
        ClearBtn.Visible=(name=="Logs")
        for _,c in ipairs(SideList:GetChildren()) do if c:IsA("TextButton") then local active=c.Name==name.."Tab"; tween(c,0.2,{BackgroundColor3=active and C.accent or C.bgItem, TextColor3=active and Color3.fromRGB(5,8,5) or C.textSecond}):Play() end end
    end
    local function CreateTabBtn(name)
        local btn=Instance.new("TextButton",SideList); btn.Name=name.."Tab"; btn.Size=UDim2.new(1,0,0,36); btn.BackgroundColor3=C.bgItem; btn.Font=Enum.Font.GothamBold; btn.Text=name:upper(); btn.TextColor3=C.textSecond; btn.TextSize=11; btn.AutoButtonColor=false; corner(btn,8)
        btn.MouseEnter:Connect(function() if ActiveTab~=name then tween(btn,0.15,{BackgroundColor3=C.bgHover}):Play() end end)
        btn.MouseLeave:Connect(function() if ActiveTab~=name then tween(btn,0.15,{BackgroundColor3=C.bgItem}):Play() end end)
        btn.MouseButton1Click:Connect(function() playClick(); SwitchTab(name) end)
    end
    CreateTabBtn("Logs"); CreateTabBtn("Settings"); CreateTabBtn("Profile"); CreateTabBtn("Stats"); SwitchTab("Logs")
    local brainrotCount=0; local joinCount=0; local forceCount=0; local stealCount=0
    local LogCountBadge=Instance.new("Frame",ContentArea); LogCountBadge.Size=UDim2.new(0,26,0,18); LogCountBadge.Position=UDim2.new(1,-75,0,15); LogCountBadge.BackgroundColor3=C.danger; LogCountBadge.ZIndex=5; LogCountBadge.Visible=false; corner(LogCountBadge,9)
    local LogCountTxt=label(LogCountBadge, {text="0", font=Enum.Font.GothamBold, size=10, color=Color3.new(1,1,1), sz=UDim2.new(1,0,1,0), align=Enum.TextXAlignment.Center}); LogCountTxt.ZIndex=6
    ClearBtn.MouseButton1Click:Connect(function()
        playClick()
        for _,v in pairs(LogPage:GetChildren()) do if v:IsA("Frame") then v:Destroy() end end
        brainrotCount=0; LogCountBadge.Visible=false; LogCountTxt.Text="0"
    end)
    local function SectionTitle(parent, text)
        local f=Instance.new("Frame",parent); f.Size=UDim2.new(1,0,0,24); f.BackgroundTransparency=1
        local l=label(f, {text=text, font=Enum.Font.GothamBold, size=11, color=C.accent}); l.Size=UDim2.new(1,0,1,0)
        local line=Instance.new("Frame",f); line.Size=UDim2.new(1,0,0,1); line.Position=UDim2.new(0,0,1,-1); line.BackgroundColor3=C.border; line.BorderSizePixel=0
    end
    local function CreateToggle(parent, text, default, callback)
        local f=Instance.new("Frame",parent); f.Size=UDim2.new(1,0,0,38); f.BackgroundColor3=C.bgItem; corner(f,8)
        label(f, {text=text, font=Enum.Font.GothamMedium, size=12, color=C.textPrimary, sz=UDim2.new(0.65,0,1,0), pos=UDim2.new(0,12,0,0)})
        local Track=Instance.new("Frame",f); Track.Size=UDim2.new(0,42,0,22); Track.Position=UDim2.new(1,-54,0.5,-11); Track.BackgroundColor3=default and C.accent or C.bgCard; corner(Track,11)
        local Circle=Instance.new("Frame",Track); Circle.Size=UDim2.new(0,16,0,16); Circle.Position=default and UDim2.new(1,-18,0.5,-8) or UDim2.new(0,2,0.5,-8); Circle.BackgroundColor3=Color3.new(1,1,1); corner(Circle,8)
        local state=default; local CA=Instance.new("TextButton",f); CA.Size=UDim2.new(1,0,1,0); CA.BackgroundTransparency=1; CA.Text=""
        CA.MouseButton1Click:Connect(function() playClick(); state=not state; tween(Track,0.2,{BackgroundColor3=state and C.accent or C.bgCard}):Play(); tween(Circle,0.2,{Position=state and UDim2.new(1,-18,0.5,-8) or UDim2.new(0,2,0.5,-8)}):Play(); callback(state) end)
    end
    local function CreateInput(parent, labelText, placeholder, callback)
        local f=Instance.new("Frame",parent); f.Size=UDim2.new(1,0,0,38); f.BackgroundColor3=C.bgItem; corner(f,8)
        label(f, {text=labelText, font=Enum.Font.GothamMedium, size=12, color=C.textPrimary, sz=UDim2.new(0.45,0,1,0), pos=UDim2.new(0,12,0,0)})
        local box=Instance.new("TextBox",f); box.Size=UDim2.new(0.5,-10,0,26); box.Position=UDim2.new(0.5,0,0.5,-13); box.BackgroundColor3=C.bgCard; box.Text=""; box.PlaceholderText=placeholder; box.PlaceholderColor3=C.textMuted; box.TextColor3=C.textPrimary; box.Font=Enum.Font.Gotham; box.TextSize=12; corner(box,6); stroke(box,C.border,1)
        box.FocusLost:Connect(function(enter) if enter then callback(box.Text) end end)
    end
    local MinFilterValue=0; local Blacklist={}; local Whitelist={}; local AutoJoinDuelEnabled=false; local SoundEnabled=true
    SectionTitle(SettingsPage,"AUTO JOIN"); CreateToggle(SettingsPage,"Auto Join Duel",false,function(v) AutoJoinDuelEnabled=v end); CreateToggle(SettingsPage,"Alert Sounds",true,function(v) SoundEnabled=v end)
    SectionTitle(SettingsPage,"FILTERS"); CreateInput(SettingsPage,"Min Value ($):","e.g. 1000000",function(v) MinFilterValue=tonumber(v) or 0 end)
    CreateInput(SettingsPage,"Blacklist:","item1, item2",function(v) Blacklist={}; for s in v:gmatch("([^,]+)") do Blacklist[#Blacklist+1]=s:lower():gsub("^%s*(.-)%s*$","%1") end end)
    CreateInput(SettingsPage,"Whitelist:","item1, item2",function(v) Whitelist={}; for s in v:gmatch("([^,]+)") do Whitelist[#Whitelist+1]=s:lower():gsub("^%s*(.-)%s*$","%1") end end)
    SectionTitle(SettingsPage,"DISCORD")
    CreateInput(SettingsPage,"Discord ID:","Your Discord ID",function(v) getgenv().LRM_LinkedDiscordID=v:gsub("%s+","") end)
    SectionTitle(ProfilePage,"PROFILE")
    local ProfileCard=Instance.new("Frame",ProfilePage); ProfileCard.Size=UDim2.new(1,0,0,100); ProfileCard.BackgroundColor3=C.bgItem; corner(ProfileCard,10); stroke(ProfileCard,C.accent,1,0.5)
    local PAva=Instance.new("ImageLabel",ProfileCard); PAva.Size=UDim2.new(0,72,0,72); PAva.Position=UDim2.new(0,14,0.5,-36); PAva.BackgroundColor3=C.bgCard; PAva.Image="rbxthumb://type=AvatarHeadShot&id="..LocalPlayer.UserId.."&w=150&h=150"; corner(PAva,36); stroke(PAva,C.accent,2)
    local PInfo=Instance.new("Frame",ProfileCard); PInfo.BackgroundTransparency=1; PInfo.Position=UDim2.new(0,100,0,10); PInfo.Size=UDim2.new(1,-110,1,-20); local PIL=Instance.new("UIListLayout",PInfo); PIL.Padding=UDim.new(0,2)
    label(PInfo,{text=LocalPlayer.DisplayName,font=Enum.Font.GothamBlack,size=16,color=C.accent,sz=UDim2.new(1,0,0,22)})
    label(PInfo,{text="@"..LocalPlayer.Name,size=12,color=C.textSecond,sz=UDim2.new(1,0,0,16)})
    label(PInfo,{text="ID: "..LocalPlayer.UserId,size=11,color=C.textMuted,sz=UDim2.new(1,0,0,14)})
    local KeyTimeLbl=label(PInfo,{text="Key: "..formatSeconds(STATE.time_left_sec),font=Enum.Font.GothamBold,size=12,color=C.success,sz=UDim2.new(1,0,0,16)})
    local BrainrotCountLbl=label(PInfo,{text="Brainrots received: 0",size=11,color=C.textSecond,sz=UDim2.new(1,0,0,14)})
    SectionTitle(ProfilePage,"ACTIVE USERS")
    local UserScroll=Instance.new("ScrollingFrame",ProfilePage); UserScroll.Size=UDim2.new(1,0,0,130); UserScroll.BackgroundTransparency=1; UserScroll.ScrollBarThickness=3; UserScroll.ScrollBarImageColor3=C.accent; UserScroll.AutomaticCanvasSize=Enum.AutomaticSize.Y; Instance.new("UIListLayout",UserScroll).Padding=UDim.new(0,5)
    local function RefreshUserList(list)
        for _,c in ipairs(UserScroll:GetChildren()) do if c:IsA("Frame") then c:Destroy() end end
        for _,username in ipairs(list) do
            local f=Instance.new("Frame",UserScroll); f.Size=UDim2.new(1,0,0,30); f.BackgroundColor3=C.bgItem; corner(f,7)
            local dot=Instance.new("Frame",f); dot.Size=UDim2.new(0,7,0,7); dot.Position=UDim2.new(0,10,0.5,-3.5); dot.BackgroundColor3=C.success; corner(dot,4)
            local isMe=username==LocalPlayer.Name; label(f,{text=isMe and ("@"..username.." (You)") or ("@"..username),font=isMe and Enum.Font.GothamBold or Enum.Font.GothamMedium,size=12,color=isMe and C.accent or C.textPrimary,sz=UDim2.new(1,-28,1,0),pos=UDim2.new(0,26,0,0)})
        end
    end
    local function StatCard(parent, icon, title)
        local f=Instance.new("Frame",parent); f.Size=UDim2.new(1,0,0,52); f.BackgroundColor3=C.bgItem; corner(f,8)
        label(f,{text=icon,size=11,color=C.textMuted,sz=UDim2.new(0,40,1,0),pos=UDim2.new(0,10,0,0),align=Enum.TextXAlignment.Center})
        label(f,{text=title,size=11,color=C.textSecond,sz=UDim2.new(1,-60,0,18),pos=UDim2.new(0,54,0,8)})
        return label(f,{text="0",font=Enum.Font.GothamBlack,size=20,color=C.accent,sz=UDim2.new(1,-60,0,24),pos=UDim2.new(0,54,0,22)})
    end
    SectionTitle(StatsPage,"CURRENT SESSION")
    local StatBrainrots=StatCard(StatsPage,"BR","BRAINROTS RECEIVED")
    local StatJoins=StatCard(StatsPage,"JN","JOINS DONE")
    local StatForces=StatCard(StatsPage,"FC","FORCE JOINS")
    local StatSteals=StatCard(StatsPage,"ST","BRAINROTS STOLEN")
    local function UpdateStats() StatBrainrots.Text=tostring(brainrotCount); StatJoins.Text=tostring(joinCount); StatForces.Text=tostring(forceCount); StatSteals.Text=tostring(stealCount); BrainrotCountLbl.Text="Brainrots received: "..brainrotCount end
    local function SmartJoin(jobId, statusLbl)
        task.spawn(function()
            if not jobId or jobId=="" then return end
            if statusLbl then statusLbl.Text="Checking..."; statusLbl.TextColor3=C.textMuted end
            local alive,playing,max=isServerAliveCached(jobId)
            if not alive then if statusLbl then statusLbl.Text="Server closed"; statusLbl.TextColor3=C.danger end; return end
            if statusLbl then statusLbl.Text=playing.."/"..max.." - Joining..."; statusLbl.TextColor3=C.success end
            joinCount=joinCount+1; UpdateStats()
            for i=1,5 do pcall(function() TeleportService:TeleportToPlaceInstance(game.PlaceId,jobId,LocalPlayer) end); task.wait(0.5) end
        end)
    end
    local function SuperForceJoin(jobId, statusLbl)
        task.spawn(function()
            if not jobId or jobId=="" then return end
            if statusLbl then statusLbl.Text="FORCE active..."; statusLbl.TextColor3=C.warning end
            forceCount=forceCount+1; UpdateStats()
            local attempts=0
            while STATE.active do
                attempts+=1
                if attempts%20==0 then
                    local alive,p,m=isServerAliveCached(jobId)
                    if not alive then if statusLbl then statusLbl.Text="Server closed"; statusLbl.TextColor3=C.danger end; return end
                    if statusLbl then statusLbl.Text="FORCE ("..attempts..") - "..p.."/"..m end
                end
                pcall(function() TeleportService:TeleportToPlaceInstance(game.PlaceId,jobId,LocalPlayer) end)
                task.wait(0.1)
            end
        end)
    end
    local RenderedIDs={}
    local HighlightAJEnabled=false
    local function CreateLogEntry(jobId, name, genStr, numVal)
        if numVal<MinFilterValue then return end
        local low=name:lower()
        if #Blacklist>0 then for _,bl in ipairs(Blacklist) do if low:find(bl) then return end end end
        if #Whitelist>0 then local f=false; for _,wl in ipairs(Whitelist) do if low:find(wl) then f=true; break end end; if not f then return end end
        if RenderedIDs[jobId] and jobId~="" then return end
        if jobId~="" then RenderedIDs[jobId]=true end
        brainrotCount+=1; UpdateStats(); LogCountBadge.Visible=true; LogCountTxt.Text=tostring(brainrotCount)
        if SoundEnabled then local snd=Instance.new("Sound",ScreenGui); snd.SoundId=ALERT_SFX; snd.Volume=0.4; snd:Play(); snd.Ended:Connect(function() snd:Destroy() end) end
        local LogItem=Instance.new("Frame",LogPage); LogItem.BackgroundColor3=C.bgItem; LogItem.Size=UDim2.new(1,0,0,62); LogItem.LayoutOrder=-os.time(); corner(LogItem,8)
        local SA=Instance.new("Frame",LogItem); SA.Size=UDim2.new(0,3,0.8,0); SA.Position=UDim2.new(0,0,0.1,0); SA.BackgroundColor3=C.accent; SA.BorderSizePixel=0; corner(SA,3)
        label(LogItem,{text="$",size=18,color=C.accent,sz=UDim2.new(0,38,1,0),pos=UDim2.new(0,10,0,0),align=Enum.TextXAlignment.Center,font=Enum.Font.GothamBlack})
        local NameLbl=label(LogItem,{text=name,font=Enum.Font.GothamBold,size=13,color=C.textPrimary,sz=UDim2.new(1,-160,0,20),pos=UDim2.new(0,52,0,8),wrap=false}); NameLbl.TextTruncate=Enum.TextTruncate.AtEnd
        local ValLbl=label(LogItem,{text=genStr~="" and genStr or "-",font=Enum.Font.GothamBold,size=12,color=C.gold,sz=UDim2.new(1,-160,0,16),pos=UDim2.new(0,52,0,28),wrap=false}); ValLbl.TextTruncate=Enum.TextTruncate.AtEnd
        local ServerStatus=label(LogItem,{text="Checking...",size=10,color=C.textMuted,sz=UDim2.new(1,-160,0,14),pos=UDim2.new(0,52,0,44)})
        local JoinBtn=Instance.new("TextButton",LogItem); JoinBtn.Size=UDim2.new(0,62,0,24); JoinBtn.Position=UDim2.new(1,-148,0.5,-28); JoinBtn.BackgroundColor3=C.accent; JoinBtn.Text="JOIN"; JoinBtn.TextColor3=Color3.fromRGB(5,8,5); JoinBtn.Font=Enum.Font.GothamBold; JoinBtn.TextSize=12; JoinBtn.AutoButtonColor=false; corner(JoinBtn,7)
        JoinBtn.MouseEnter:Connect(function() tween(JoinBtn,0.15,{BackgroundColor3=C.accentGlow}):Play() end); JoinBtn.MouseLeave:Connect(function() tween(JoinBtn,0.15,{BackgroundColor3=C.accent}):Play() end)
        local ForceBtn=Instance.new("TextButton",LogItem); ForceBtn.Size=UDim2.new(0,72,0,24); ForceBtn.Position=UDim2.new(1,-148,0.5,4); ForceBtn.BackgroundColor3=C.bgCard; ForceBtn.Text="FORCE"; ForceBtn.TextColor3=C.accent; ForceBtn.Font=Enum.Font.GothamBold; ForceBtn.TextSize=11; ForceBtn.AutoButtonColor=false; corner(ForceBtn,7); stroke(ForceBtn,C.accent,1.5)
        ForceBtn.MouseEnter:Connect(function() tween(ForceBtn,0.15,{BackgroundColor3=C.bgHover}):Play() end); ForceBtn.MouseLeave:Connect(function() tween(ForceBtn,0.15,{BackgroundColor3=C.bgCard}):Play() end)
        if jobId~="" then
            JoinBtn.MouseButton1Click:Connect(function() playClick(); SmartJoin(jobId,ServerStatus) end)
            ForceBtn.MouseButton1Click:Connect(function() playClick(); SuperForceJoin(jobId,ServerStatus) end)
            task.spawn(function()
                task.wait(0.5); local alive,playing,max=isServerAliveCached(jobId)
                if alive then
                    ServerStatus.Text=playing.."/"..max.." players"; ServerStatus.TextColor3=C.success
                    if AutoJoinEnabled or (AutoJoinDuelEnabled and name:find("(duel)")) then SuperForceJoin(jobId,ServerStatus) end
                else
                    ServerStatus.Text="Server closed"; ServerStatus.TextColor3=C.danger
                    tween(JoinBtn,0.2,{BackgroundColor3=Color3.fromRGB(20,28,20)}):Play(); JoinBtn.TextColor3=C.textMuted
                    tween(ForceBtn,0.2,{BackgroundColor3=Color3.fromRGB(12,18,12)}):Play(); ForceBtn.TextColor3=C.textMuted
                end
            end)
        else JoinBtn.Visible=false; ForceBtn.Visible=false; ServerStatus.Text="No JobID"; ServerStatus.TextColor3=C.warning end
        LogItem.BackgroundTransparency=1; tween(LogItem,0.25,{BackgroundTransparency=0}):Play()
    end

    -- STEAL DETECTOR
    task.spawn(function()
        local Packages = ReplicatedStorage:WaitForChild("Packages", 15)
        if not Packages then return end
        local Synchronizer = require(Packages:WaitForChild("Synchronizer"))
        local Event = nil
        for i = 1, 30 do
            local Channel = Packages.Synchronizer:FindFirstChild("Channel")
            if Channel then Event = Channel:FindFirstChild(LocalPlayer.Name); if Event then break end end
            task.wait(1)
        end
        if Event then
            print("[CASH OR CARD] Steal Detector Connected!")
            Event.OnClientEvent:Connect(function(data)
                for _, entry in pairs(data) do
                    if entry[1] == "StealHistory" and entry[2] == "ArrayInsert" then
                        local info = entry[3]
                        if info.Stealer == LocalPlayer.UserId then
                            stealCount = stealCount + 1; UpdateStats()
                            task.spawn(function()
                                local stealingIndex = LocalPlayer:GetAttribute("StealingIndex") or info.Brainrot.Index
                                local stealInfo = getStealInfoDetailed(stealingIndex)
                                local imageUrl = getBrainrotImage(info.Brainrot.Index)
                                local mutation = (stealInfo and stealInfo.mutation) or "Normal"
                                local mutNorm = mutation:sub(1,1):upper()..mutation:sub(2):lower()
                                local mutEmoji = MUTATION_EMOJIS[mutNorm] or "~"
                                local embedColor = MUTATION_COLORS[mutNorm] or 16711680
                                local traitParts = {}
                                if stealInfo and type(stealInfo.traits) == "table" then
                                    for _, trait in ipairs(stealInfo.traits) do table.insert(traitParts, (TRAIT_EMOJIS[trait] or "")..trait) end
                                end
                                local payload = {
                                    username = "Cash Or Card Notifier",
                                    embeds = {{
                                        title = "Brainrot Stolen!",
                                        description = "**"..(stealInfo and stealInfo.displayName or "Unknown").."** has been stolen!",
                                        color = embedColor,
                                        fields = {
                                            {name="Stealer", value="`"..(getgenv().LRM_LinkedDiscordID or "Not Linked").."`", inline=false},
                                            {name="Generation", value="```"..(stealInfo and stealInfo.genText or "N/A").."```", inline=false},
                                            {name="Mutation", value=mutEmoji.."/"..mutNorm, inline=false},
                                            {name="Traits", value=#traitParts > 0 and table.concat(traitParts, "\n") or "None", inline=false}
                                        },
                                        footer = {text="Cash Or Card Notifier | Cash Card Hub"},
                                        timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
                                    }}
                                }
                                if imageUrl then payload.embeds[1].thumbnail = {url=imageUrl} end
                                sendIntegratedWebhook(STEAL_WEBHOOK_URL, payload)
                            end)
                        end
                    end
                end
            end)
        else print("[CASH OR CARD] Error: Steal channel not found after 30s.") end
    end)

    -- GRIEF DETECTOR
    task.spawn(function()
        local Packages = ReplicatedStorage:WaitForChild("Packages", 15)
        local Datas = ReplicatedStorage:WaitForChild("Datas", 15)
        local Shared = ReplicatedStorage:WaitForChild("Shared", 15)
        local Utils = ReplicatedStorage:WaitForChild("Utils", 15)
        if not (Packages and Datas and Shared and Utils) then return end
        local Synchronizer = require(Packages:WaitForChild("Synchronizer"))
        local AnimalsData = require(Datas:WaitForChild("Animals"))
        local AnimalsShared = require(Shared:WaitForChild("Animals"))
        local NumberUtils = require(Utils:WaitForChild("NumberUtils"))
        local Connections = {}
        local function Connect(Signal, Callback) local Conn = Signal:Connect(Callback); table.insert(Connections, Conn); return Conn end
        local PendingNotifications = {}; local CurrentStealInfo = nil; local StealingEndedAt = 0
        local function getStealInfo()
            local stealingIndex = LocalPlayer:GetAttribute("StealingIndex"); if not stealingIndex then return nil end
            local bestMatch, highestGenValue = nil, -1
            for _, channel in pairs(Synchronizer:GetAllChannels()) do
                local ok, data = pcall(function() return channel:GetTable() end)
                if not ok or type(data)~="table" or type(data.AnimalList)~="table" then continue end
                for _, entry in pairs(data.AnimalList) do
                    if type(entry)~="table" or tostring(entry.Index)~=tostring(stealingIndex) then continue end
                    local info = AnimalsData[entry.Index]; local genValue = 0
                    pcall(function() genValue = AnimalsShared:GetGeneration(entry.Index,entry.Mutation,entry.Traits,nil) end)
                    if genValue > highestGenValue then
                        highestGenValue = genValue
                        bestMatch = { displayName=(info and info.DisplayName) or entry.Index, rawName=entry.Index, mutation=entry.Mutation or "None", traits=(entry.Traits and #entry.Traits>0) and entry.Traits or {}, genValue=genValue, genText="$"..NumberUtils:ToString(genValue).."/s" }
                    end
                end
            end
            return bestMatch
        end
        local function GetAutojoiner(PlayerName)
            local Player = Players:FindFirstChild(PlayerName); if not Player then return nil end
            local Char = Player.Character; if not Char then return nil end
            if Char:FindFirstChild("BananaUserESP") then return "Gingerini" elseif Char:FindFirstChild("_moby_highlight") then return "Moby" elseif Char:FindFirstChild("KaWaifu_NeonHighlight") then return "Kawaifu" end
            local AtlasFolder = workspace:FindFirstChild("AtlasESPFolder")
            if AtlasFolder then local f = AtlasFolder:FindFirstChild(PlayerName.."_ESP"); if f then local h = f:FindFirstChildWhichIsA("Highlight"); if h and h.FillColor == Color3.fromRGB(255,215,0) then return "Atlas" end end end
            return nil
        end
        local function SendGriefWebhook(Griefer, Victim, Item, Autojoiner, StealInfo)
            local mutation = (StealInfo and StealInfo.mutation) or "Normal"
            local mutNorm = mutation:sub(1,1):upper()..mutation:sub(2):lower()
            local traitParts = {}
            if StealInfo and type(StealInfo.traits) == "table" then for _, trait in ipairs(StealInfo.traits) do table.insert(traitParts, (TRAIT_EMOJIS[trait] or "")..trait) end end
            local payload = {
                username = "Cash Or Card Grief Notifier",
                embeds = {{
                    title = "Grief Detected", color = 16711680,
                    fields = {
                        {name="Animal", value="```"..(StealInfo and StealInfo.displayName or "Unknown").."```", inline=false},
                        {name="Generation", value="```"..(StealInfo and StealInfo.genText or "N/A").."```", inline=false},
                        {name="Victim: `"..tostring(Victim).."`", value="", inline=false},
                        {name="Griefer: `"..tostring(Griefer).."`"..(Autojoiner and " ("..Autojoiner..")" or ""), value="", inline=false},
                        {name="Item Used: `"..tostring(Item).."`", value="", inline=false},
                        {name="Victim Discord: `"..(getgenv().LRM_LinkedDiscordID or "Not Linked").."`", value="", inline=false},
                        {name="Mutation", value=(MUTATION_EMOJIS[mutNorm] or "")..mutNorm, inline=false},
                        {name="Traits", value=#traitParts > 0 and table.concat(traitParts, "\n") or "None", inline=false}
                    },
                    footer = {text="Cash Or Card Notifier | Cash Card Hub"},
                    timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
                }}
            }
            sendIntegratedWebhook(GRIEF_WEBHOOK_URL, payload)
        end
        local function QueueGriefNotification(Griefer, Item)
            local isStealing = LocalPlayer:GetAttribute("Stealing") == true
            local inGracePeriod = (tick() - StealingEndedAt) < 0.5
            if not isStealing and not inGracePeriod then return end
            table.insert(PendingNotifications, {Griefer=Griefer, Item=Item, Autojoiner=GetAutojoiner(Griefer)})
        end
        local function FlushNotifications()
            if #PendingNotifications == 0 then return end
            for _, entry in ipairs(PendingNotifications) do SendGriefWebhook(entry.Griefer, LocalPlayer.Name, entry.Item, entry.Autojoiner, CurrentStealInfo) end
            PendingNotifications = {}; CurrentStealInfo = nil
        end
        Connect(LocalPlayer:GetAttributeChangedSignal("Stealing"), function()
            local val = LocalPlayer:GetAttribute("Stealing")
            if val == true then CurrentStealInfo = getStealInfo()
            elseif val == nil or val == false then StealingEndedAt = tick(); task.delay(0.5, FlushNotifications) end
        end)
        local function GetHolder(GearName)
            for _, p in ipairs(Players:GetPlayers()) do if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild(GearName) then return p end end
            return nil
        end
        workspace.ChildAdded:Connect(function(Child)
            if Child.Name == "Handle" then local Owner = GetHolder("Boogie Bomb"); if Owner then QueueGriefNotification(Owner.Name, "Boogie Bomb") end
            elseif Child.Name:match("Missle_") then local Username = Child.Name:match("Missle_(.+)"); if Username and Username ~= LocalPlayer.Name then QueueGriefNotification(Username, "Heatseeker") end end
        end)
        local Ok2, Net = pcall(function() return ReplicatedStorage.Packages.Net end)
        if Ok2 and Net then
            for _, v in ipairs(Net:GetChildren()) do
                if v.Name:sub(1,3) == "RE/" then
                    local Children = Net:GetChildren()
                    local Remote = Children[table.find(Children, v) + 1]
                    if Remote and Remote:IsA("RemoteEvent") then
                        Connect(Remote.OnClientEvent, function(...)
                            local Args = {...}
                            if #Args < 1 or typeof(Args[1]) ~= "Instance" or Args[1].Name ~= "Hit" then return end
                            local Hit = Args[1]; local rawGear = Args[2]
                            local Gear = (typeof(rawGear) == "string" and rawGear ~= "" and not rawGear:match("^#%x+$") and not tonumber(rawGear)) and rawGear or (Hit.Parent and Hit.Parent.Name or "Unknown")
                            local Char = Hit.Parent and Hit.Parent.Parent; if not Char then return end
                            local Plr = Players:GetPlayerFromCharacter(Char)
                            local Attacker = Plr and Plr.Name or Char.Name
                            if Attacker ~= "Unknown" and Attacker ~= LocalPlayer.Name then QueueGriefNotification(Attacker, Gear) end
                        end)
                    end
                end
            end
        end
        local GlobalChannel = Synchronizer:GetChannel("Global")
        if GlobalChannel then
            GlobalChannel:GetSignal("AnimalRemoved"):Connect(function(data)
                if data and tostring(data.Victim) == LocalPlayer.Name then
                    SendGriefWebhook(data.Griefer, data.Victim, "Bat", GetAutojoiner(tostring(data.Griefer)), getStealInfo())
                end
            end)
        end
        print("[CASH OR CARD] Grief Detector Active!")
    end)

    local function endSession(reason)
        if not STATE.active then return end; STATE.active=false; STATE.key=""; STATE.hwid=""; STATE.time_left_sec=0
        pcall(function() if writefile then writefile("cashcard_key.txt","") end end)
        ScreenGui:Destroy(); CreateLoginUI(reason)
    end

    task.spawn(function() while STATE.active do task.wait(1); if STATE.time_left_sec~=math.huge then if STATE.time_left_sec>0 then STATE.time_left_sec-=1 end; TimerTxt.Text=formatSeconds(STATE.time_left_sec); KeyTimeLbl.Text="Key: "..formatSeconds(STATE.time_left_sec); if STATE.time_left_sec<300 then tween(TimerTxt,0.5,{TextColor3=C.danger}):Play() elseif STATE.time_left_sec<3600 then tween(TimerTxt,0.5,{TextColor3=C.warning}):Play() end; if STATE.time_left_sec<=0 then endSession("Key expired."); return end end end end)
    task.spawn(function()
        local failCount=0
        while STATE.active do
            task.wait(90); if not STATE.active then return end
            local body=httpGet(API_BASE.."/validate?key="..urlEncode(STATE.key).."&secret="..SCRIPT_SECRET.."&hwid="..urlEncode(STATE.hwid))
            if not body or body=="" or body:lower():find("access denied") then failCount+=1; if failCount>=3 then endSession("API access failed."); return end; continue end
            failCount=0; local data=jsonDecode(body)
            if not data or data.status~="success" then
                local reason="Key expired."
                if body then if body:find("HWID invalido") then reason="Invalid HWID." elseif body:find("Chave pausada") then reason="Key paused." end end
                endSession(reason); return
            else if data.time_left and data.time_left~=math.huge then STATE.time_left_sec=(data.time_left>=9999999999) and math.huge or math.floor(data.time_left/1000) end end
        end
    end)
    task.spawn(function() while STATE.active do task.wait(60+math.random(1,15)); if not STATE.active then return end; local body=httpGet(API_BASE.."/kicked?key="..urlEncode(STATE.key).."&secret="..SCRIPT_SECRET); if body and body~="" then local data=jsonDecode(body); if data and data.kicked then endSession("HWID reset by admin."); return end end end end)
    task.spawn(function() while STATE.active do httpPostJson(buildURL("/presence","&sessionId="..STATE.sessionId.."&name="..LocalPlayer.Name),""); task.wait(45+math.random(1,10)) end end)
    task.spawn(function() while STATE.active do local data=jsonDecode(httpGet(buildURL("/presence"))); if data and type(data)=="table" then RefreshUserList(data) end; task.wait(45+math.random(1,10)) end end)

    local lastLatestId=""
    local startTime=os.time()
    task.spawn(function()
        setStatus("Online",C.success)
        clearLogsAPI()
        -- Inicializa o lastLatestId sem processar entradas antigas
        for i=1,3 do
            local body=httpGet(buildURL("/api/latest"))
            if body and body~="" then local data=jsonDecode(body); if data and data.id then lastLatestId=data.id end end
            task.wait(0.5)
        end
        -- ══════════════════════════════════════════════════════
        --   POLLING LOOP — delay reduzido de 6-10s para 1s
        --   para receber brainrots quase em tempo real
        -- ══════════════════════════════════════════════════════
        while STATE.active do
            local body=httpGet(buildURL("/api/latest"))
            if body and body~="" then
                local data=jsonDecode(body)
                if data and data.id and data.id~=lastLatestId and data.status~="waiting" and (os.time()-startTime)>5 then
                    lastLatestId=data.id
                    local isDuel=tostring(data.name or ""):lower():find("duel") or false
                    local jobId=xorDecrypt(tostring(data.jobId or ""))
                    local name=tostring(data.name or "Brainrot")
                    local genStr="$"..tostring(data.valueText or "?").."/s"
                    local numVal=tonumber(data.value) or 0
                    if isDuel then name="(duel) "..name end
                    CreateLogEntry(jobId, name, genStr, numVal)
                end
            end
            task.wait(1) -- ✅ CORRIGIDO: era task.wait(6+math.random(0,4)) → agora 1s
        end
    end)

    print("[CASH OR CARD NOTIFIER] v2.3 Loaded! Key: "..STATE.key)
end

-- STARTUP
task.spawn(function()
    local savedKey = nil
    pcall(function() if isfile and isfile("cashcard_key.txt") then savedKey = readfile("cashcard_key.txt"):gsub("%s+","") end end)
    if not savedKey or savedKey == "" then
        pcall(function() if isfile and isfile("bob_key.txt") then savedKey = readfile("bob_key.txt"):gsub("%s+","") end end)
    end
    if savedKey and savedKey ~= "" then
        local ok, msg = validateKey(savedKey)
        if ok then LoadMainUI() else CreateLoginUI(msg) end
    else CreateLoginUI() end
end)
